{% extends 'base.html.twig' %}

{% block title %}Edit Article
{% endblock %}

{% block body %}
	<h1>Edit Article</h1>
	<a href="{{ path('blog_index') }}" class="btn btn-primary">back to list</a>

	{{ include('blog/_form.html.twig', {'button_label': 'Update'}) }}


	{{ include('blog/_delete_form.html.twig') }}
{% endblock %}
