{% extends 'base.html.twig' %}

{% block title %}Article
{% endblock %}

{% block body %}
	<h1>Article</h1>
	<a href="{{ path('blog_index') }}" class="btn btn-primary">back to list</a>
	<a href="{{ path('blog_edit', {'id': article.id}) }}" class="btn btn-secondary">edit</a>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ article.id }}</td>
			</tr>
			<tr>
				<th>Titre</th>
				<td>{{ article.titre }}</td>
			</tr>
			<tr>
				<th>Content</th>
				<td>{{ article.content }}</td>
			</tr>

		</tbody>
	</table>


	{{ include('blog/_delete_form.html.twig') }}
{% endblock %}
